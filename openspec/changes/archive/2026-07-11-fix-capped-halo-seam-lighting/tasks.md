## 1. Capped Halo Selection

- [x] 1.1 Trace the LOD1 cap-pressure path in `voxel-generator.ts` and add bounded, deterministic receiving-boundary geometry samples or spatial buckets derived from generated opaque quads.
- [x] 1.2 Replace broad-volume-only halo ranking under cap pressure with edge- and corner-local selection that retains sources whose configured falloff reaches visible boundary geometry at the relevant Y/Z location.
- [x] 1.3 Preserve existing uncapped ordering, binary emitter layout, halo flags, default light semantics, and bounded record-cap behavior.
- [x] 1.4 Increment voxel cache semantics so disk and in-memory payload caches cannot serve entries generated by the prior capped-halo selection policy.

## 2. Diagnostics And Regression Coverage

- [x] 2.1 Extend `scripts/validate-voxel-seams.ts` with a dense cap-pressure fixture whose same-edge-distance halo sources differ by Y/Z locality.
- [x] 2.2 Execute the production voxel mesh worker in the seam validator and compare normalized emissive attributes for matching position-and-normal seam vertices from adjacent LOD1 payloads.
- [x] 2.3 Assert that the fixture retains every source needed by the compared seam vertices, reports cap-selection context, and fails when the worker-baked delta exceeds the encoding tolerance.
- [x] 2.4 Reproduce the `1/640/5376` to `1/768/5376` seam at nighttime, verify initial and settled LOD1 loads, and record payload source membership plus matched-vertex bake values.

## 3. Documentation And Verification

- [x] 3.1 Update `docs/architecture-overview.md`, `docs/server-specification.md`, and `docs/client-specification.md` with the cap-pressure halo retention and worker-level seam-validation behavior.
- [x] 3.2 Run `npm run validate:voxel-seams` and confirm both capped and uncapped fixtures pass.
- [x] 3.3 Run `npm run check`, `npm run check:knip`, `npm run typecheck`, and `npm run build`.

## 4. Coarse LOD Follow-up

- [x] 4.1 Reproduce the reported LOD2 seam at `2/512/5376` to `2/768/5376` and identify owning-summary-only payload composition as the missing cross-boundary source path.
- [x] 4.2 Compose radius-filtered same-LOD neighboring summary representatives into coarse payloads, preserve runtime accent ownership through halo flags, and include all dependencies in cache identity and live invalidation.
- [x] 4.3 Increment voxel cache semantics and add a production-worker LOD2 adjacent-payload regression with matching position-and-normal seam vertices.
- [x] 4.4 Update architecture, server, and client documentation and run full verification.
